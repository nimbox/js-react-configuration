// const [values, setValues] = useState([]);

// en el on blur
// if (nullable) {
//     if (values.length === 0) {
//         dispatch(null);
//     } else {
//         validate(values);
//     }
// } else {
//     validate(values);
// }
